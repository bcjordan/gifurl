<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "GifURL" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag "gifurl" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, link_to("GifURL #{yield(:title)}", '/') %>
      <%= yield %>
    </div>
  </body>
</html>
